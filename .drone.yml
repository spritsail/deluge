---
kind: pipeline
name: deluge-dev

platform:
  os: linux
  arch: amd64

steps:
- name: build
  pull: always
  image: spritsail/docker-build
  settings:
    repo: deluge-dev

- name: test
  pull: always
  image: spritsail/docker-test
  settings:
    repo: deluge-dev
    delay: 3
    verbose: true
    curl_opts: -o/dev/null -sS
    curl: ':8112'
    # Add a user and attempt connecting to the daemon
    exec_post: |
      echo "username:password:10" >> /config/auth;
      sleep 2; # wait for deluged to detect auth change
      deluge-console -l /dev/stdout -L deluge "connect localhost username password; quit"
          | tee /dev/stderr
          | grep -qF "Connection was closed cleanly"

- name: publish
  pull: always
  image: spritsail/docker-publish
  settings:
    from: deluge-dev
    repo: spritsail/deluge
    tags:
    - dev
    - "%label io.spritsail.version.deluge"
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
  when:
    branch:
    - dev
    event:
    - push

---
kind: signature
hmac: 402777b62b8bc9a2a190e4b7437bb6ba21f30cbbab612413e68f524ac59bef68

...
